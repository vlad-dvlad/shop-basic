import React from 'react';

const Header = () => {
    return (
        <header className='w-full bg-[var(--accent)] p-4'>
            <h1 className='text-lg font-bold color-[var(--text)]'>
                Shop basic
            </h1>
        </header>
    );
};

export default Header;